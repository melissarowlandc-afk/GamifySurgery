{
  "name": "@gamify-surgery/clinical-authoring",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "validate:example": "npm run build && node dist/cli.js --public-safe examples/synthetic-workspace.json"
  }
}
