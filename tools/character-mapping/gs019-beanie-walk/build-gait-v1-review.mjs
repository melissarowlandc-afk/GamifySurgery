import { createHash } from 'node:crypto';
import { existsSync, mkdirSync, readFileSync, writeFileSync } from 'node:fs';
import { execFileSync } from 'node:child_process';
import path from 'node:path';

const repo=path.resolve(import.meta.dirname,'../../..'),candidateRoot=path.join(repo,'artifacts/character-movement/gs019-beanie-walk/west-candidate-v1'),reviewRoot=path.join(candidateRoot,'review'),manifestFile=path.join(candidateRoot,'manifest.json'),phases=['01','02','03','04','05','06','07','08'];
const manifest=JSON.parse(readFileSync(manifestFile,'utf8')),standingFile=path.join(repo,manifest.pins.approvedStanding.file),westFiles=phases.map(phase=>path.join(candidateRoot,manifest.frames[phase].file)),eastFiles=phases.map(phase=>path.join(candidateRoot,manifest.eastFrames[phase].file));
for(const file of[manifestFile,standingFile,...westFiles,...eastFiles])if(!existsSync(file))throw new Error(`missing review input: ${file}`);
const sha256=value=>createHash('sha256').update(value).digest('hex'),relative=file=>path.relative(repo,file).replaceAll('\\','/'),mime={png:'image/png',webp:'image/webp'};
function makePayload(extension,files){const assets={},keyFor=file=>{const bytes=readFileSync(file),key=sha256(bytes);if(!assets[key])assets[key]=`data:${mime[extension]};base64,${bytes.toString('base64')}`;return key;};return{cadenceMs:180,phases,assets,refs:{standing:keyFor(files.standing),west:files.west.map(keyFor),east:files.east.map(keyFor)}};}
const template=readFileSync(path.join(import.meta.dirname,'gait-v1-review-template.html'),'utf8'),render=payload=>template.replace('__GS019_BEANIE_V1_PAYLOAD__',JSON.stringify(payload));
let sourceFiles={standing:standingFile,west:westFiles,east:eastFiles},payload=makePayload('png',sourceFiles),encoding='png-dedup',encodedAssets=null;
if(Buffer.byteLength(render(payload))>=1_000_000){const encodedRoot=path.join(reviewRoot,'lossless-webp');mkdirSync(encodedRoot,{recursive:true});const cache=new Map(),encode=(source,name)=>{const digest=sha256(readFileSync(source));if(cache.has(digest))return cache.get(digest);const target=path.join(encodedRoot,`${name}.webp`),program="from PIL import Image; import sys; original=Image.open(sys.argv[1]).convert('RGBA'); original.save(sys.argv[2], format='WEBP', lossless=True, exact=True, method=6); assert original.tobytes()==Image.open(sys.argv[2]).convert('RGBA').tobytes()";execFileSync('python',['-c',program,source,target],{stdio:'pipe'});cache.set(digest,target);return target;};encodedAssets={standing:encode(standingFile,'standing-west'),west:westFiles.map((file,index)=>encode(file,`west-${phases[index]}`)),east:eastFiles.map((file,index)=>encode(file,`east-${phases[index]}`))};sourceFiles=encodedAssets;payload=makePayload('webp',sourceFiles);encoding='lossless-webp-dedup';}
const html=render(payload);if(Buffer.byteLength(html)>=1_000_000)throw new Error(`review fragment remains over 1MB: ${Buffer.byteLength(html)} bytes`);mkdirSync(reviewRoot,{recursive:true});const reviewFile=path.join(reviewRoot,'brown-beanie-walk-review.html');writeFileSync(reviewFile,html);
const pin=file=>({file:relative(file),sha256:sha256(readFileSync(file))}),pins={schemaVersion:'gs019-beanie-walk/review-v1',review:{file:relative(reviewFile),sha256:sha256(html),bytes:Buffer.byteLength(html),encoding,uniqueAssets:Object.keys(payload.assets).length},inputs:{manifest:pin(manifestFile),standing:pin(standingFile),west:Object.fromEntries(westFiles.map((file,index)=>[phases[index],pin(file)])),east:Object.fromEntries(eastFiles.map((file,index)=>[phases[index],pin(file)]))},encodedAssets:encodedAssets&&{standing:pin(encodedAssets.standing),west:Object.fromEntries(encodedAssets.west.map((file,index)=>[phases[index],pin(file)])),east:Object.fromEntries(encodedAssets.east.map((file,index)=>[phases[index],pin(file)]))}};
writeFileSync(path.join(reviewRoot,'input-pins.json'),JSON.stringify(pins,null,2)+'\n');console.log(JSON.stringify({status:'PASS',review:relative(reviewFile),bytes:Buffer.byteLength(html),encoding,uniqueAssets:Object.keys(payload.assets).length},null,2));
