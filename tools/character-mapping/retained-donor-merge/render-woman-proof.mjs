import { chromium } from 'playwright';import { pathToFileURL } from 'node:url';import { resolve } from 'node:path';const repo=resolve(import.meta.dirname,'../../..'),out=resolve(repo,'artifacts/character-movement/retained-donor-merge/woman-proof'),html=resolve(out,'woman-proof.html'),browser=await chromium.launch({headless:true,executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe'});try{for(const [name,dpr] of [['native',1],['map-scale',2]]){const page=await browser.newPage({viewport:{width:1100,height:760},deviceScaleFactor:dpr});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(pathToFileURL(html).href,{waitUntil:'load'});await page.waitForSelector('body[data-ready="true"]');await page.screenshot({path:resolve(out,`woman-proof-${name}.png`),fullPage:true});if(errors.length)throw new Error(errors.join('; '));await page.close()}console.log(JSON.stringify({native:'woman-proof-native.png',mapScale:'woman-proof-map-scale.png',pageErrors:0}))}finally{await browser.close()}
